## Learned User Preferences

- Prefers Cursor chat over CLI for LoL board/betting; never push `python -m …` commands for day-to-day board usage
- Fair-odds-first: on draft, lead with model fair odds; book decimal > fair ⇒ +EV; when book odds are present, show Actual alongside Fair so edge size is obvious; odds only later for GRADE/Kelly
- Prefer Slip Composer text paste over screenshot OCR for drafts and live state; screenshots often misread champs and dragons; NL mode over multi-field fine-tune for fast decisions; separate Copy live (LIVE only) vs Copy for Cursor/Dump (draft+LIVE)
- Trust the user’s stated draft over tab/HUD OCR; when they correct a read, acknowledge, restate, and recompute — do not argue; newest champs (e.g. Locke) must stay in the board even with no OE Elo (neutral prior) — never drop them
- Count dragon icons carefully per side (do not round; 3 ≠ 2); do not confuse grub shields with dragons; current-season void grubs max is 3 per side (not 6)
- Full Kelly is OK on small recreational bankrolls when the user explicitly asks for it
- Cashout advice should be data-driven (live survival / model EV vs offered cashout), not gut-only; during live games prioritize speed (short live-glance) over full board reloads
- When odds are moving, give actionable thresholds (“if book ≥ X → take/hold”); for kill totals, also a fair-odds O/U ladder across nearby lines when the market is jumping
- Simples / bet-slip paste → one-view board (favorite %, fair odds, kills bucket/median); when grading a ticket, order by edge
- Research/external writeups & public Scryglass (@ninarin00/koi): isolation-controlled ±pp (2 decimals, explicit +/-); adversarial multi-lens review before shipping (incl. chart/figure style+content); figures = type-only research aesthetic (no cartoon icons/champ portraits); voice = hedges + full sentences (no AI slogan/thesis cadence; follow `.cursor/rules/ninarin00-voice.mdc`); when drafting public replies, preserve the user’s draft structure/voice — fix facts, don’t wholesale rewrite format; keep reply drafts short and in the user’s register (no format/voice rewrite, no verbosity); LoL wiki–grounded item/stat mechanics over illustrative hypos; when a Live Stats dump exists prefer exact JSONL numbers over wiki ranges; champ tierlists/strength boards = Elo-controlled ΔWR (not raw WR) with counterability in the tier score, prefer current-patch + sample floors + why-notes + impact/conversion when available; Blind/Counter is OE matchup-shape proxy (no pick-order), not literal P1/P2; regional scopes Europe=LEC only, Americas=LCS only, INTL=MSI and EWC separated; public methodology copy must avoid internal module/function names; do not invent unpublished thread framing; no internal tooling leaks; concise layman conclusions; PDF+LaTeX when delivering articles; Scryglass is non-betting (Elo/stats + warehouse-reproducible packs 2025–2026 + void-grubs companion), plain-language labels over bare p*, formulas in LaTeX, light/dark/system theme (system default), Leaguepedia-like H2H, editorial/Miller aesthetic (avoid clinical/hospital chrome and financial-dashboard slop), in-page PDF, model-vs-actual browse checks (kills O/U, winner, draft WR) must mark outcomes correctly
- Prefer model-vs-public disagreement edges (capitalize when soft public/book favorites disagree with the model) over short-odds public favorites
- Prefers player-aggregated team strength over static team Elo when rosters move; public Dual Elo ladders should be region/league-scoped (a global mix misranks regional orgs vs major-region elites) with sortable columns and team→player→champ drilldown

## Learned Workspace Facts

- LoL offline betting research engine lives in `lol_kills`; draft boards via `build_board` / `format_fair_odds` in `lol_kills/board.py`
- Slip Composer UI is at `web/composer/` (`index.html`); NL + Live pad; paste block is ground truth for chat; bet-slip one-view: `.cursor/skills/bet-slip-view` → `lol_kills.skills.slip_view`; live map-WR/objectives: `.cursor/skills/live-glance` → `lol_kills.skills.live_glance` (needs `p_pre` / Pre %)
- GRADE / Kelly / combo grading lives in `lol_kills/econ.py` (`grade_bet` / `grade_combo`)
- Draft Score is league-calibrated (v3 classic + v4 composite): `lol_kills.draft_phase_score.draft_score_composite` emits @10/@15/@20/@25 curve + Flores beatdown/control roles; fit via `lol_kills.research.draft_phase_beatdown` → `data/lol/models/draft_phase_beatdown.json`. Classic scalar still in `lol_kills.draft_score` / `draft_wr_calibration.json`. Beatdown framing: early-damage side must convert by ~15–20; control side weathers late — live_win uses phase-bucket `draft_edge` for the current minute. Champ tierlists: Blade-Chest counterability (`lol_kills.research.champ_tierlist_blade_chest` → `champ_tierlist_*_blade_chest.json` / `champ_tierlist_*_by_region.json` / `blade_chest_role_matchups.json`) plus impact/conversion (`lol_kills.research.champ_impact` → `champ_impact.json`); CSV exports under `data/lol/models/tierlists_csv/`; scopes LEC / LCS / MSI / EWC (separated)
- Multi-angle draft dynamics for betting: `lol_kills.draft_dynamics.analyze_draft_dynamics` (tempo/inevitability, goldfish, siege/TF/split, pilot carry, initiative, combo vs fair, variance seat, spike calendar, Magriel) → board `dynamics`; OE checks via `lol_kills.research.draft_dynamics_validate` → `draft_dynamics_validation.json`
- Ratings stack: Dual Elo (`lol_kills/ratings/dual_elo.py`); Player Dual-Elo → `player_ratings.parquet` / snapshot with board `p_player_elo` + `p_strength_blend`; Elo→WR calibration (`calibrate_elo_wr.py` → `elo_wr_calibration.json`) uses hotter player-scale mapping than classic 400
- Team aliases include KC → Karmine Corp, DK → Dplus Kia, and MKOI/KOI → Movistar KOI (`web/composer/teams.json`, `lol_kills/etl/aliases.py`); Locke is in champ aliases/archetypes/tierlist with neutral OE prior until warehouse coverage exists
- Primary warehouse is Oracle’s Elixir CSVs; agent-only refresh via `lol_kills.refresh_warehouse` / `lol_kills.pipeline`; Riot Match-V5 timeline cache at `data/lol/warehouse/timelines/` (rate limits ~20/1s, 100/2min per routing value)
- Public Scryglass app at `apps/lol-atlas` (Vercel): Dual Elo ladders, OE parquet pack browse/H2H (pack years 2025–2026 under `public/packs/`), void-grubs research companion; DuckDB-WASM needs absolute origin URLs for parquet (relative `/packs/...` fails)
- Void-grubs research: isolation (`grubs_isolation_study.py` / `side_objective_edges.py`), contest decision numbers/report (`grubs_decision_numbers.json`, `grubs_decision_report.md`), fight-prob helper (`grubs_fight_probability.py`), mechanical intrinsic-value package (`grubs_intrinsic_value.py` → `grubs_intrinsic_value.json`; PDF via `grubs_intrinsic_pdf.py` → `output/pdf/void_grubs_scrap_value_and_contest_rationality.pdf`). Leave-farm packages must be position-realistic (MS×distance, camp timers); article headline 2w+plate may not map when the ADC is already midmap/river. Contest case studies separate team gold lead L (+ OLS direction over trailing windows) from fighting strength (shop-priced inventory vs purse, inactive combatants). Do not confuse estimands: OE trailing-team leave-mix breakeven (~24%) ≠ article opportunity-cost break-even ladder (two-wave reference ~58.9%; rises with leave-farm and when ahead) — Scryglass `/grubs` should surface the article estimand for lay readers, not the leave-mix table as the headline. Article p(map win) = side-neutral gold@10 associational logit conversion, not causal/draft-true WR
- Soft public-favorite proxy (brand + form heat): `lol_kills/research/public_soft_signal.py`. Live boards emit `public_edge` with flags (`fade-heat→brand`, `playerElo+`, `draft+`, `confGap≥10`) and actions FADE_CANDIDATE / PASS_ANTI / AGREE_STRONG. Disagreement playbook: `public_disagree_edges_2026.json`.
- Riot Live Stats JSONL (rfc461) via `lol_kills/etl/riot_esports_events.py`: ~1s champ positions + contest/kill events (not `.rofl`); `stats_update` gold/XP/items for leave/contest math; no AoE hit lists. Pro `LOLTMNT*` population needs partner/GRID access; single-game dumps unlock Tier 1–2 case studies under `data/lol/warehouse/esports_events/`
